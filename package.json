{
  "name": "dns-server",
  "version": "1.0.0",
  "description": "Authoritative DNS server for personal domains",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "ts-node src/server.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "dns-packet": "^5.6.1",
    "mongodb": "^6.10.0"
  },
  "devDependencies": {
    "@types/dns-packet": "^5.6.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.0"
  }
}
